import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="bg-gray-900 text-white p-2 text-center max-h-(--cb-hdr-max-hgt) min-h-(--cb-hdr-min-hgt)"></header>
  );
};

export default Header;